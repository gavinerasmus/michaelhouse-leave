# Michaelhouse Leave System - Environment Configuration

# ==================== Backend Selection ====================
# Set to 'true' to use Google Sheets instead of PostgreSQL
# Great for testing and demos without database setup
USE_GOOGLE_SHEETS=false

# Set to 'true' for simplified OAuth (personal Google account)
# Set to 'false' for service account (requires Google Cloud setup)
USE_SIMPLE_OAUTH=false

# ==================== Google Sheets - Simple OAuth (Personal Account) ====================
# Only needed if USE_GOOGLE_SHEETS=true and USE_SIMPLE_OAUTH=true
# Path to OAuth client credentials JSON (from Google Console)
GOOGLE_OAUTH_CREDENTIALS_PATH=/path/to/client_secrets.json

# ==================== Google Sheets - Service Account ====================
# Only needed if USE_GOOGLE_SHEETS=true and USE_SIMPLE_OAUTH=false
# Path to your service account credentials JSON file
GOOGLE_SHEETS_CREDENTIALS_PATH=/path/to/service-account-credentials.json

# ==================== Google Sheets - Common ====================
# Your Google Sheet ID (from the URL between /d/ and /edit)
GOOGLE_SHEET_ID=your-sheet-id-here

# ==================== PostgreSQL Database ====================
# Only needed if USE_GOOGLE_SHEETS=false
DB_HOST=localhost
DB_NAME=michaelhouse_leave
DB_USER=leave_user
DB_PASSWORD=your-secure-password
DB_PORT=5432

# ==================== AI Configuration ====================
# Anthropic Claude API key for conversational agent
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here

# ==================== Email Configuration ====================
# Only needed if using email bridge
LEAVE_EMAIL=leave@michaelhouse.org
LEAVE_EMAIL_PASSWORD=your-app-specific-password
IMAP_SERVER=imap.gmail.com
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
IMAP_PORT=993

# ==================== API Configuration ====================
# Flask API port (default: 8090)
FLASK_PORT=8090

# Flask environment (development/production)
FLASK_ENV=development

# ==================== Leave System Configuration ====================
# Default term (auto-detected if not set)
# CURRENT_TERM=Term 1
# CURRENT_YEAR=2025

# ==================== Logging ====================
# Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Agent log directory
AGENT_LOG_DIR=logs/agent-logs
